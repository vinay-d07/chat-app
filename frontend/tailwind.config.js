/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Extend colors if needed, but standard Tailwind palette has everything
      },
    },
  },
  plugins: [],
}
